{
  "name": "sintag-ltd-server",
  "version": "1.0.0",
  "description": "Server for sintag private limited and I have created it for my assignment",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm build --output public"
  },
  "author": "S M SHAFI",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
